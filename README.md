para ejecutar el proyecto se debe descragra el código fuente en un equipo con java 11+
y se debe compilar con mvn clean install, comando ejecutado desde un terminal en al carpeta
contienen el archivo pom.xml

el puerto configurado para despliegue es el 8081

y se puede probar el api generado en la siguiente ruta
http://localhost:8081/swagger-ui/index.html#/Usuarios

la interfaz de usuario del swagger indica el JSON que se debe enviar en la peticion
